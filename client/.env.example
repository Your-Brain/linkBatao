# Backend API URL
# Leave as /api during local development (Vite proxies /api to http://localhost:5000)
# Set to your production backend URL when deploying (e.g. https://auralink-api.onrender.com/api)
VITE_API_URL=/api
