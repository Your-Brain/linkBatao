{
  "name": "auralink-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Full-Stack Anonymous Media Discovery & Link Management Platform",
  "scripts": {
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "seed": "npm run seed --prefix server",
    "start": "npx concurrently \"npm run dev:server\" \"npm run dev:client\""
  }
}